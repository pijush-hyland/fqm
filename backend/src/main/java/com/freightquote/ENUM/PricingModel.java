package com.freightquote.ENUM;

public enum PricingModel {
    AIR_PER_KG,           // Air freight: price per kilogram
    WATER_FCL_PER_CONTAINER, // FCL: price per container
    WATER_LCL_PER_CBM     // LCL: price per cubic meter (W/M)
}
